**[English](/README.md)** | **[Русский](/docs/README.ru.md)** | **[中文](/docs/README.zh.md)**

## Описание

Легковесное приложение для связи в реальном времени с низкой задержкой. Обеспечивает **голосовую связь, текстовые сообщения и аппаратно-ускоренную демонстрацию экрана** в одном исполняемом файле со встроенным сервером - отдельная установка сервера не требуется.

<p align="center">
  <img src="/docs/MainWindow-ru.png" alt="Скриншот главного окна">
</p>

## Основные возможности

- **Голосовая связь** с подавлением шума (RNNoise) и маскировкой потери пакетов (Opus PLC)
- **Демонстрация экрана** на 30 fps с аппаратным кодированием H.264 (NVENC → AMF → libx264 в качестве запасного варианта)
- **Аудиостек CoreAudio** (WASAPI/MMDevice) для захвата и воспроизведения с низкой задержкой
- **Адаптивное воспроизведение аудио** с оценкой джиттера и динамическим размером буфера
- **Адаптивное воспроизведение демонстрации экрана** с покадровым расчетом времени и логикой целевого буфера
- **Автоматический проброс портов UPnP** через Open.NAT (опционально, в один клик)
- **Сквозное шифрование**: обмен ключами ECDH → сессионный ключ AES-256-CBC для каждого участника
- **Двухканальный транспорт TCP + UDP** (LiteNetLib): управление через TCP, медиа через UDP
- **Прозрачное переподключение**: окно в 5 секунд при потере связи, без повторной аутентификации
- **Встроенный сервер** - запускается только когда пользователь выбирает создание сессии; никаких затрат при запуске в остальных случаях
- **Архитектура MVVM** с динамическими DI-контейнерами (Microsoft.Extensions.DependencyInjection)

## Компоненты системы

### Клиент (WPF .NET 8)
- Интерфейс с тёмной темой, русская и китайская локализация
- Захват аудио WASAPI (микрофон + системный loopback) с подавлением шума RNNoise
- Кодек Opus (режим VOIP, кадры 480 сэмплов / 10 мс, PLC при потере пакетов)
- Кодирование/декодирование H.264 через FFmpeg (avcodec, swscale)
- Захват экрана через Desktop Duplication API (SharpDX) с наложением курсора
- Сетевой клиент TCP/UDP с прозрачным переподключением

### Встроенный сервер (DLL .NET 8)
- Создание сессий, проверка пароля, управление пользователями
- Маршрутизация пакетов MSG/CMD - сервер никогда не расшифровывает содержимое сообщений
- Ретрансляция медиа (аудио/видео) между участниками
- Сессионный ключ AES для каждой сессии, распределяемый через XOR с общим секретом ECDH каждого клиента
- Арбитраж слота демонстрации экрана (один докладчик одновременно)

### Отдельный сервер для Linux
- Отдельный репозиторий: **[talknado-server-linux](https://github.com/SodiumCXI/talknado-server-linux)**
- Та же обработка сессий и ретрансляция медиа, что и у встроенного сервера, автономный бинарник linux-x64
- Установка:
```bash
curl -fsSL https://raw.githubusercontent.com/SodiumCXI/talknado-server-linux/main/install.sh | bash
```

## Безопасность

Рукопожатие при подключении выполняется в семь этапов:

1. **Проверка версии** - несовместимые клиенты отклоняются до обмена любыми ключами
2. **Обмен ключами ECDH** - клиент и сервер обмениваются публичными ключами; каждая сторона независимо получает общий секрет
3. **Проверка пароля** - SHA-256(пароль) шифруется с помощью AES-256-CBC, используя общий секрет
4. **Передача сессионного ключа** - сервер выполняет XOR 32-байтового сессионного ключа с уникальным общим секретом клиента; XOR самообратим, поэтому дополнительный шаг расшифровки не требуется
5. **Обмен идентификацией** - имя пользователя и назначенный ID пользователя, оба зашифрованы сессионным ключом
6. **Регистрация UDP** - зашифрованный userId отправляется через LiteNetLib; сервер подтверждает с помощью `#UCC`
7. **Синхронизация состояния** - сервер передаёт список участников и текущее состояние демонстрации экрана

Начиная с шага 4, весь трафик (управляющий и медиа) шифруется сессионным ключом (AES-256-CBC).

## Как использовать через интернет

### Вариант A - автоматический (UPnP)
При запуске сервера отметьте **"Проброс портов UPnP"**. Talknado попытается автоматически пробросить порт через Open.NAT. Проброс удаляется при остановке сервера. Требуется статический публичный IP.

### Вариант B - ZeroTier (работает всегда)
1. Установите **[ZeroTier](https://www.zerotier.com/)** на каждом устройстве.
2. Все участники присоединяются к одной сети ZeroTier (Network ID).
3. Запустите Talknado и введите ключ подключения из приложения хоста.